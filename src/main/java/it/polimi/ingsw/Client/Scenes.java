package it.polimi.ingsw.Client;

/**
 * Enumeration class that define possible GUI's scenes
 */
public enum Scenes {
    mainMenuScene,
    loginScene,
    gameScene,
    scoreboardScene,
    loadingScene,
    creditsScene
}
